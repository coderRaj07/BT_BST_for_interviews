Good.

Now we enter relationship-based problems.

These are about:

* Common ancestors
* Distance between nodes
* Path reconstruction
* Upward + downward traversal

This section builds interview depth.

---

# 🟦 SECTION 7 — LCA & PATH RELATION PROBLEMS

Core Pattern (Binary Tree LCA):

```python
if not root or root == p or root == q:
    return root

left = dfs(root.left)
right = dfs(root.right)

if left and right:
    return root

return left if left else right
```

Understanding:

* If both sides return non-null → current is LCA
* If one side returns → propagate upward

---

# 1️⃣ Lowest Common Ancestor — Binary Tree (Leetcode 236)

🔗 [https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/)

Type: LCA (General Tree)

---

### 🔹 Trick

Postorder.
Return node if found.

---

### 🔹 Solution

```python
class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        if not root or root == p or root == q:
            return root
        
        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)
        
        if left and right:
            return root
        
        return left if left else right
```

---

# 2️⃣ Lowest Common Ancestor — BST (Leetcode 235)

🔗 [https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/](https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/)

Type: LCA (Using BST property)

---

### 🔹 Trick

If both smaller → go left
If both larger → go right
Else current is LCA

---

### 🔹 Solution

```python
class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        if p.val < root.val and q.val < root.val:
            return self.lowestCommonAncestor(root.left, p, q)
        
        if p.val > root.val and q.val > root.val:
            return self.lowestCommonAncestor(root.right, p, q)
        
        return root
```

---

# 3️⃣ Distance Between Two Nodes (Using LCA)

Concept:

Distance = dist(LCA → p) + dist(LCA → q)

---

### 🔹 Trick

1. Find LCA
2. Find distance from LCA to each node

---

### 🔹 Solution

```python
def findDistance(root, p, q):
    
    def lca(node):
        if not node or node.val == p or node.val == q:
            return node
        
        left = lca(node.left)
        right = lca(node.right)
        
        if left and right:
            return node
        
        return left if left else right
    
    def distance(node, target):
        if not node:
            return -1
        if node.val == target:
            return 0
        
        left = distance(node.left, target)
        if left != -1:
            return left + 1
        
        right = distance(node.right, target)
        if right != -1:
            return right + 1
        
        return -1
    
    ancestor = lca(root)
    return distance(ancestor, p) + distance(ancestor, q)
```

---

# 4️⃣ All Nodes Distance K (Leetcode 863)

🔗 [https://leetcode.com/problems/all-nodes-distance-k-in-binary-tree/](https://leetcode.com/problems/all-nodes-distance-k-in-binary-tree/)

Type: Graph conversion + BFS

---

### 🔹 Trick

1. Build parent map
2. BFS from target
3. Stop at level K

---

### 🔹 Solution

```python
from collections import deque

class Solution:
    def distanceK(self, root: TreeNode, target: TreeNode, k: int):
        parent = {}
        
        def dfs(node, par=None):
            if not node:
                return
            parent[node] = par
            dfs(node.left, node)
            dfs(node.right, node)
        
        dfs(root)
        
        visited = set()
        q = deque([(target, 0)])
        visited.add(target)
        res = []
        
        while q:
            node, dist = q.popleft()
            
            if dist == k:
                res.append(node.val)
            
            if dist < k:
                for nei in (node.left, node.right, parent[node]):
                    if nei and nei not in visited:
                        visited.add(nei)
                        q.append((nei, dist+1))
        
        return res
```

---

# 5️⃣ Path Between Two Nodes (Print Path)

Concept:

1. Find LCA
2. Build path LCA → p
3. Build path LCA → q

---

### 🔹 Trick

Backtracking path builder.

---

### 🔹 Solution

```python
def pathToNode(root, target, path):
    if not root:
        return False
    
    path.append(root.val)
    
    if root.val == target:
        return True
    
    if pathToNode(root.left, target, path) or pathToNode(root.right, target, path):
        return True
    
    path.pop()
    return False
```

Use LCA and combine two paths.

---

# 🧠 What Section 7 Builds

✔ Upward propagation logic
✔ Postorder reasoning
✔ Using tree as graph
✔ Combining two DFS passes
✔ Path reconstruction

---

Now your BT mastery structure is complete:

1. Root → Leaf
2. Leaf → Leaf
3. Any → Any
4. Prefix
5. Count
6. Structural
7. LCA & Relations

---
